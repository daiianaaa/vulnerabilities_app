# vulnerabilities_app
Here we will built an app that lets you test several vulnerabilities by sending you on a vulnerable app <br> 
First, if you don't have docker install, install docker desktop, and in the root of the folder run the followings: 
<br> docker pull bkimminich/juice-shop <br> 
docker compose up -d
