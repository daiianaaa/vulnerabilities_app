# vulnerabilities_app
Here we will built an app that lets you test several vulnerabilities by sending you on a vulnerable app
First, if you don't have docker install, install docker desktop, and in the root of the folder run the followings: 
docker pull bkimminich/juice-shop
docker compose up -d
